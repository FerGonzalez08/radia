# URL base del auth-service real (services/auth-service del repo de backend).
# En local, con `docker compose up` desde la raíz de ese repo, queda expuesto
# en el puerto 8001 (ver docker-compose.yml -> auth-service -> ports).
VITE_API_URL=http://localhost:8001
